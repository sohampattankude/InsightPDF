// Simple README for the React frontend
# Multimodal PDF Q&A Frontend

This React app lets users upload a PDF, ask questions, and view answers from the multimodal backend.

## How to run

1. Open a terminal in the `frontend` folder.
2. Run `npm install` to install dependencies.
3. Run `npm start` to launch the app.
4. The app will open at `http://localhost:3000`.

Make sure your FastAPI backend is running at `http://localhost:8000`.
